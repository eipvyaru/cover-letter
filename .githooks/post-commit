#!/bin/sh
set -u

echo
echo "Running post-commit release readiness checks..."
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ./scripts/verify-release.ps1
status=$?

if [ "$status" -ne 0 ]; then
  echo "The commit was created, but it is NOT READY for GitHub or VPS deployment."
fi

exit "$status"
